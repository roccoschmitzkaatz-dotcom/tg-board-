// TG Board — Supabase-Konfiguration (BEISPIEL)
// Bogdan/Backend: diese Datei nach `js/supabase-config.js` kopieren
// und mit den echten Werten aus dem Supabase-Projekt füllen.
// (supabase-config.js gehört NICHT ins Repo → steht in .gitignore.)
window.TG_SUPABASE = {
  url:     "https://DEIN-PROJEKT.supabase.co",   // Project URL
  anonKey: "DEIN-ANON-PUBLIC-KEY"                 // anon public key (kein service_role!)
};
